# Web layer only. Manim, numpy, pycairo, ffmpeg and LaTeX all come from the
# manimcommunity/manim base image, so nothing here may pull manim in again.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20
